# @package _global_
defaults:
  - default

dataset:
  ev_repr_name: 'rff_dt=50_D=16_sigma=1.0_pol=two_channel'

model:
  backbone:
    input_channels: 64
    embed_dim: 64
    resolution_hw: [256, 320]
    gaussian_kernel: 5
    factor: 1.2
  fpn:
    depth: 0.67
